# Handover — 2026-04-22 Context Reset

**Purpose.** This doc is the single source of truth a fresh Claude session
needs to resume ds-meal work after `/clear`. Read it top-to-bottom and you will
know exactly where we are, what's live, and what's next.

## Where we are

- **Phase 4 in progress.** Slice A shipped, Slice B coded + tested + committed
  but **not yet redeployed**. Slice C is next.
- **Plan doc:** `docs/PHASE-4-EXECUTION-PLAN.md` (the 8-slice execution plan).
- **Head commit on `main`:** `4ca431c` — *feat(slice-b): Clerk sign-in live +
  facility dashboard*.
- **Branch strategy:** trunk-based solo. Every slice commits straight to
  `main`. No feature branches.
- **Repo:** https://github.com/seoninja13/direct-supply-meal (synced).
- **VPS path:** `/opt/direct-supply-meal/`.
- **Subdomain:** https://ds-meal.dulocore.com.

## What's live vs what's only in GitHub

| Layer | State |
|---|---|
| Slice A code (recipe browse) | ✅ Live on https://ds-meal.dulocore.com |
| Slice A container | ✅ `ds-meal` container healthy, Up ~4 hours |
| Slice B code (Clerk auth + dashboard) | ✅ Committed + pushed (`4ca431c`) |
| Slice B container | ❌ Not yet rebuilt; `/sign-in` 404s against Slice A image |
| Slice B tests | ✅ 22 passing locally (14 Slice A + 8 Slice B) |

Attempting to rebuild at ~18:35 UTC hit `ENOSPC` — the VPS disk was saturated.
User ran cleanup (docker prune, journald vacuum). Disk now 48% used. No
redeploy was attempted after cleanup — that's waiting for the next session.

## Slice A — DONE

Live URLs:
- `GET /` — landing
- `GET /health` → `{"status":"ok","service":"ds-meal"}`
- `GET /api/v1/health` — JSON twin
- `GET /recipes` — 10 recipes in HTML
- `GET /recipes/{id}` — detail with nutrition grid
- `GET /recipes/{id}/ingredients?servings=N` — scaled via `scale_recipe()`
- `GET /api/v1/recipes` + `/{id}` + `/{id}/ingredients` — JSON twins

Coverage: scaling 94%, main 93%, recipes 70%, public 87%. Overall 46%
(dragged by Slice C-G stubs at 0%; ramps as we ship).

## Slice B — CODE DONE, DEPLOY PENDING

On GitHub at `4ca431c` (not yet visible at https://ds-meal.dulocore.com because
the container hasn't been rebuilt):

- `app/auth/clerk_middleware.py` — PyJWT + JWKS RS256 verification. Replaced
  `python-jose` with `pyjwt[crypto]`. 10-min JWKS TTL cache.
- `app/auth/dependencies.py` — `require_login` reads `__session` cookie or
  `Authorization: Bearer`, loads User, returns `CurrentUser`.
  `require_facility_access(resource_facility_id, user)` tenancy guard.
- `app/auth/provisioning.py` — three-case provisioning:
  (1) existing by clerk_user_id → idempotent,
  (2) seed placeholder matched by email → upgrade clerk_user_id in place,
  (3) no row → match Facility.admin_email or raise NotOnAllowlist.
- `app/routes/public.py` — `GET /sign-in` (derives AccountPortal URL from the
  publishable key via server-side b64 decode), `GET /sign-in/callback`
  (JS handoff via `Clerk.session.getToken()`), `POST /sign-in/exchange`
  (verifies JWT, provisions, sets HttpOnly cookie), `POST /sign-out`.
- `app/routes/facility.py` — gated `GET /facility/dashboard`,
  `GET /api/v1/facility/me` JSON twin.
- `app/templates/auth/sign_in.html` + `app/templates/auth/callback.html`
  (pure JS, loads `@clerk/clerk-js@5` from the tenant's CDN).
- `app/templates/facility/dashboard.html` — placeholder "active orders" empty
  state. Slice C fills it.
- `scripts/seed_db.py` — now loads `fixtures/facilities.json` (5 facilities;
  only Riverside SNF has admin_email) and seeds a User id=1 placeholder with
  `clerk_user_id='__unprovisioned__'` so future demo-order FKs resolve.
- `tests/fixtures/clerk_jwt_helpers.py` — ephemeral RS256 keypair + threaded
  HTTP server serving JWKS + `mint_session_token()`.
- `tests/integration/test_auth.py` — 8 tests: unauthenticated 401,
  invalid-token 401, allowlisted exchange 200, non-allowlisted 403,
  full flow, sign-out 303, api_me, expired token 401.
- `pyproject.toml` — swapped `python-jose[cryptography]` → `pyjwt[crypto]`.
- Cookie `secure=False` for http:// tests to round-trip; flip to True in
  Phase 2 via a `SECURE_COOKIES` setting.

### Before the user presses redeploy, TWO things matter:

1. **Clerk dashboard**: Google provider must be toggled ON in the DS-Meal app
   under *User & Authentication → Social Connections*. Without it the
   AccountPortal shows "no social providers" and sign-in is impossible. This
   is a 2-min task on https://dashboard.clerk.com/.
2. **Deploy commands (on VPS, after disk cleanup):**
   ```
   cd /opt/direct-supply-meal
   git pull
   docker compose up -d --build
   docker ps | grep ds-meal
   curl -I https://ds-meal.dulocore.com/sign-in
   ```

## Open items — must-address before Slice C starts

| # | Item | Blocks |
|---|---|---|
| 1 | Rebuild ds-meal container (Slice B redeploy) | Slice C (dashboard needs facility data + orders) |
| 2 | Enable Google provider in the Clerk DS-Meal app | Slice B first real sign-in; Slice C tests against real session |
| 3 | `.github/workflows/ci.yml` not pushed (PAT needed `workflow` scope) | Only matters at Slice H for RealAuth CI enforcement |
| 4 | SSH deploy key on GitHub is installed but not recognized (works for CF API, not for git push). Using PAT for all pushes. | Not a blocker — PAT path works. Phase 2 cleanup. |

## What Slice C ships next

Scope (from `docs/PHASE-4-EXECUTION-PLAN.md` §6 Slice C):

1. Dashboard gets real active-orders summary (5 seeded demo orders, 4 statuses).
2. Order history at `/orders` — paginated, filter by status, status badges.
3. Order detail at `/orders/{id}` — OrderLine breakdown + status timeline +
   6-segment progress bar (partials already stubbed).
4. Calendar at `/calendar?year=&month=` — stdlib `calendar.monthcalendar()`
   grid, status-colored dots per delivery.
5. Cross-facility access returns 403 via `require_facility_access`.
6. Unit tests: state machine (all 7 legal + illegal transitions),
   `build_month_grid` edge cases, `list_orders_for_facility` pagination.
7. Integration tests + 1 Playwright E2E.

Estimate: 5 hours of focused work. Dependencies: Slice B deployed + seeded.

Key service modules to implement:
- `app/services/orders.py` — state machine + `list_orders_for_facility` +
  `get_order_with_timeline` (DEFER `generate_from_meal_plan` to Slice E per G10).
- `app/services/calendar_view.py` — month grid builder.
- Seed extension: residents + demo_orders + OrderStatusEvent histories.

## How to resume in the next session

Say **"resume Slice B deploy"** and:

1. The new session sees this handover via `git pull`.
2. First action: rebuild and verify. `cd /opt/direct-supply-meal && git pull &&
   docker compose up -d --build && docker ps | grep ds-meal && curl -I
   https://ds-meal.dulocore.com/sign-in` should return 200 HTML.
3. Manually test live sign-in with `admin@dulocore.com` once Google is
   enabled in Clerk.
4. Mark task #13 complete, move to Slice C per §6 of the execution plan.

## Save-cycle record

- Committed Slice B at `4ca431c` on 2026-04-22.
- Pushed to `main` on GitHub at 2026-04-22 ~18:31 UTC.
- This handover committed as a reviewable artifact per CLAUDE.md.
- Linear update **skipped** (per Phase 4 plan — we don't wire Linear until
  Phase 2 graduation; git log is the audit trail for Phase 1).
